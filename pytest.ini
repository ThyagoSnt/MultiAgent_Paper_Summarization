# pytest.ini
[pytest]
log_cli = true
log_cli_level = INFO
markers =
    integration: marks tests as integration (may hit external systems or LLMs)
    slow: marks tests as slow (may take longer to run)
